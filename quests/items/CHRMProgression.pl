sub EVENT_SCALE_CALC {
    # This is supposed to scale on purchased expansions Orignal thru Planes of Power
    # which would be 4 expansion but since Titanium is the lowest client and it already
    # has 6 expanstions past Planes of Power we will just put this script for max
    $questitem->SetScale(1);
} 