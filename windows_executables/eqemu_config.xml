<?xml version="1.0"?>
<server>
     <world>
        <shortname>MyServerShort</shortname>
        <longname>[PROJECT AXCLASSIC] MyServerName</longname>
                <!-- Don't use unless needed -->
		<!-- <address>axclassic.com</address>  -->
		<!-- <localaddress>192.168.2.148</localaddress>  -->
		
		<!-- Loginserver information.  -->
		<!-- You can add more Login Servers  -->
		<!-- Your own login server still retains the same account/passwords -->
        	<loginserver1>
			<host>login.axclassic.com</host>
			<port>5999</port>
			<account>myAccount</account>
			<password>myPassword</password>
		</loginserver1>

		<loginserver2>
			<host>eqemulator.net</host>
			<port>5998</port>
			<account>myOtherAccount</account>
			<password>myOtherPassword/password>
		</loginserver2>
		
		<unlocked></unlocked>
		<!-- Sets the shared key used by zone/launcher to connect to world -->
                <key>makeAKeyHere</key>

		    <!-- Sets the ip/port for the tcp connections.  Both zones and console (if enabled).  Defaults are shown -->
		    <tcp ip="127.0.0.1" port="9000" telnet="enable"/>

                <!-- Enable and set the port for the HTTP service. -->
                <http port="9090" enabled="true" mimefile="mime.types" />
        </world>

                <!-- Database configuration, replaces db.ini. -->
        <database>
                <host>localhost</host>
                <port>3306</port>
                <username>eq</username>
                <password>myPassword</password>
                <db>ax_classic</db>
        </database>

	<!-- Launcher Configuration -->
	<launcher>
		<!-- <logprefix>logs/zone-</logprefix> -->
		<!-- <logsuffix>.log</logsuffix> -->
		<!-- <exe>zone.exe or ./zone</exe> -->
		<timers restart="60000" reterminate="20000"/>
	</launcher>

</server>
